{% extends 'base.html.twig' %}

{% block main %}
     <div class="container">
        <h1>Item creation</h1>

            {{ form_start(form) }}
                {{ form_widget(form) }}
                     <div class="form-group">
                         <div class="col-sm-offset-2 col-sm-10">
                             <input type="submit" class="btn btn-default" value="Create"/>
                         </div>
                     </div>
            {{ form_end(form) }}

            <ul>
                <li>
                    <a href="{{ path('linkedin_index') }}">Back to the list</a>
                </li>
            </ul>
     </div>
{% endblock %}